import { ADD, EDIT, DELETE, FILEADD } from "./actions"

const Reducer = (state, action) => {
    switch (action.type) {
        

        default:
            return state
    }
}

export default Reducer